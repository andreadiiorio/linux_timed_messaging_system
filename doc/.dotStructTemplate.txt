//Andysnake96
// dot <name>.dot -Tsvg | display

digraph g {

compound=true	//allow [inter] cluster edges (edge opt lhead<-ltail )
graph[ rankdir = "LR"];	//left to right direction of graph as a GraphAttribute
node0 [
	label = "node0|data"
	shape = record
];
node1 [
	label = "node1|data"
	shape = record
];
node2 [
	label = "node2|data"
	shape = record
];
//sub graph def as separate entity with noded/edges/options
subgraph cluster1 { Andrea [ label="Andysnake96|AndreaDiIorio" shape=record]  Andrea -> B  color =blue}
node3 [
	label = "node3|data 1\n data1.1 | data 2 | data 3 " 
	shape = record
];

//links
node0->node3 [label="0->3"]	//edge with label
node1->node2
node2->node3 
// next node is connected to node 2 & 1; boundary 
node3 -> { node2 node1 } [dir=both color="red:blue"]	//edge to multiple nodes with bi colored edge
node3 -> {B} [lhead=cluster1]		//EDGE TO CLUSTER


}
